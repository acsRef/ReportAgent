"""Memory 领域层（伞形 plan §二·二：「长期保存什么」）。

P4a 建 `conversation.py`（Conversation Memory 引擎）+ `manager.py`（L3 write seam）。
P4b/P4c 逐步补 `semantic.py` / `query.py` / `policy.py`（infra/memory 归位）。

本包是 domain/application 层；persistence 实现在 `app/infra/memory/`。
"""
